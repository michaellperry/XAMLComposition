<UserControl
    x:Class="XAMLComposition.Views.CategoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XAMLComposition.Views"
    xmlns:c="using:XAMLComposition.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="Auto" />
            <ColumnDefinition
                Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <c:IconButton
            Content="User Groups"
            Width="90"
            Height="90"
            Margin="10"
            Icon="{StaticResource IconButtons.UserGroup}" />
        <c:IconButton
            Content="Recruiting"
            Grid.Column="1"
            Width="90"
            Height="90"
            Margin="10"
            Icon="{StaticResource IconButtons.Recruiting}" />
    </Grid>
</UserControl>
